import * as React from 'react';
import Typography from '@mui/material/Typography';

export default function NotFoundPage() {
  return (
    <Typography variant="h1" py={2} fontSize={{ xs: '1rem', sm: '2rem', md: '3rem' }}>
      Page Not Found
    </Typography>
  );
}
